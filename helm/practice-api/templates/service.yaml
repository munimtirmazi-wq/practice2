apiVersion: v1
kind: Service
metadata:
  name: {{ include "practice-api.fullname" . }}
  labels:
    app: {{ include "practice-api.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "practice-api.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
